const fs = require('fs'); let css = fs.readFileSync('src/app/globals.css', 'utf8'); let idxStart = css.indexOf('background-image: url(\"data:image'); let idxEnd = css.indexOf('background-size: 500px;'); if (idxStart > -1 && idxEnd > -1) { css = css.substring(0, idxStart) + 'background-image: url(\"/topography.svg\");\n    ' + css.substring(idxEnd); fs.writeFileSync('src/app/globals.css', css); console.log('Fixed CSS'); } else { console.log('Could not find boundaries', idxStart, idxEnd); }
